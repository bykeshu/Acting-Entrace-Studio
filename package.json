{
  "name": "acting-entrance-studio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:cloud": "esbuild cloud/cloud.js --bundle --minify --format=iife --target=es2020 --outfile=cloud.bundle.js"
  },
  "dependencies": {
    "firebase": "12.19.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
